import React from 'react'
import { Box, Typography } from '@mui/material'

export default function Feedback(){
  return (
    <Box>
      <Typography variant="h5" sx={{ mt: 2 }}>Feedback</Typography>
      <Typography sx={{ mt: 1 }}>Feedback page placeholder.</Typography>
    </Box>
  )
}
