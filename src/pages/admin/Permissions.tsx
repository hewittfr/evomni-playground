import React from 'react'
import { Box, Typography } from '@mui/material'

export default function Permissions(){
  return (
    <Box>
      <Typography variant="h5" sx={{ mt: 2 }}>Permission Admin</Typography>
      <Typography sx={{ mt: 1 }}>Manage permissions (placeholder).</Typography>
    </Box>
  )
}
