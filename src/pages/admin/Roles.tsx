import React from 'react'
import { Box, Typography } from '@mui/material'

export default function Roles(){
  return (
    <Box>
      <Typography variant="h5" sx={{ mt: 2 }}>Role Admin</Typography>
      <Typography sx={{ mt: 1 }}>Manage roles (placeholder).</Typography>
    </Box>
  )
}
